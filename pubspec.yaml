name: pilipili
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.2
  go_router: ^15.1.2
  # go_router: 
  #   path: plugins/go_router-2.5.7
  device_info_plus: ^9.0.0
  package_info_plus: ^8.3.1
  connectivity_plus: ^6.0.0
  video_player: ^2.9.5
  # video_player: 
  #   path: plugins/video_player-2.5.1
  # video_player_platform_interface: ^5.1.4
  provider: ^6.0.1
  shelf: ^1.2.0
  flutter_screenutil: ^5.9.3 # 屏幕尺寸适配
  pull_to_refresh: ^2.0.0 # 下拉刷新
  swipe_cards: ^2.0.0+1
  visibility_detector: ^0.4.0+2
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  encrypt: ^5.0.1
  crypto: ^3.0.1
  bot_toast: ^4.0.1
  shared_preferences: ^2.0.8
  country_code_picker: any
  hex: ^0.2.0
  isolated_worker: ^0.1.0
  easy_debounce: ^2.0.1
  qr_flutter: ^4.0.0
  url_launcher: ^6.0.16
  permission_handler: ^11.3.1
  app_installer: ^1.3.1
  universal_html: ^2.0.8
  image_picker: ^1.1.2
  heic_to_jpg: ^0.2.0
  fluttertoast: ^9.0.0
  image_gallery_saver: ^2.0.3
  pedantic: ^1.0.0
  flutter_html: any
  html_unescape: any
  shelf_static: ^1.1.0
  flutter_native_splash: ^2.4.1
  frhooks: ^2.0.0
  url_strategy: ^0.3.0
  preload_page_view: ^0.2.0
  photo_view: ^0.15.0
  fijkplayer: ^0.11.0
  extended_nested_scroll_view: ^6.2.1
  animated_size_and_fade: ^5.1.1
  # video_player_web_hls:
  #   git:ƒƒ
  #     url: git@172.105.200.120:rn/video_player_web_hls-0.1.11_6.git
  #     ref: fe05017
  waterfall_flow: ^3.0.2
  wakelock_plus: ^1.0.0
  card_swiper: ^3.0.1
  lpinyin: ^2.0.3
  common_utils: ^2.1.0
  webviewx_plus: 0.3.3
  flutter_inappwebview: ^6.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.1.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
flutter_native_splash:
  android: true
  ios: true
  web: false
  android_gravity: 'fill'
  ios_content_mode: scaleAspectFill
  image: 'assets/images/started.png'
  image_dark: 'assets/images/started.png'
  background_image: 'assets/images/started.png'
  background_image_dark: 'assets/images/started.png'
  fullscreen: true
# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/gif/
    # - assets/images/pili_12/
    - assets/images/comics/
    - assets/images/2023/
    - assets/images/2023/bottomTab/
    - assets/images/2024/bottomTab/
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: NumberFont
      fonts:
        - asset: fonts/number.ttf
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
